import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'funtasticelemts.app',
  appName: 'FuntasticElements',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
